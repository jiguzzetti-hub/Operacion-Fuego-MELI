server:
    port: 8082
fireOperation:
spring:
    main:
        allow-bean-definition-overriding: true
    application:
        name: fireOperation
    devtools:
        restart:
            enabled: true
